
Template.userPill.helpers({
    'labelClass': function () {
        /**Aqui se añade la clase CSS necesaria en cada caso al label del autocompletado, segun el estado de conexión
         del usuario correspondiente**/
    }
});
